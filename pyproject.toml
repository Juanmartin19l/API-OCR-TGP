[project]
name = "paddle"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "paddlepaddle==3.2.2",
    "paddleocr>=3.3.0",
]

[tool.uv]
index-url = "https://www.paddlepaddle.org.cn/packages/stable/cpu/"
